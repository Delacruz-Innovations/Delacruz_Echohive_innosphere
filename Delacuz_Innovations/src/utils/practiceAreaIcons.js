import { BrainCircuit, Cpu, Lightbulb, ShieldCheck, Target, Truck, Users, Workflow } from 'lucide-react';

// Maps the `icon` key stored per practice area in ServicesData.json to its lucide-react component
const practiceAreaIcons = {
  Target,
  Workflow,
  BrainCircuit,
  ShieldCheck,
  Cpu,
  Lightbulb,
  Users,
  Truck,
};

export default practiceAreaIcons;
